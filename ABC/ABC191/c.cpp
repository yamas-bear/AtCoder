#include <bits/stdc++.h>
using namespace std;
using ll = long long;
using P = pair<int, int>;
#define rep(i, n) for (int i = 0; i < (n); i++)

int main()
{
    ll h, w, cnt = 0, flg = 0;
    cin >> h >> w;
    ll tyouten[h][w];
    rep(i, h) rep(j, w) cin >> tyouten[i][h];
    rep(i, h) rep(j, w)
    {
        if ()
    }

    return 0;
}